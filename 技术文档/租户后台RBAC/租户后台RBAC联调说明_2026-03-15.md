# 租户后台 RBAC 联调说明（2026-03-15）

## 目标

本次实现的是 `http://127.0.0.1:3002` 租户后台自己的 RBAC 管理，和平台后台 `http://127.0.0.1:3000` 完全隔离。

不影响以下内容：

- 平台后台 `admin_roles / permissions / role_permissions`
- 平台后台 `RBAC管理`
- App 用户表 `tenant_user`
- App 用户 KYC 表 `tenant_user_kyc`

## 新增菜单

租户后台新增顶级菜单：

- `RBAC管理`
  - `用户管理`
  - `角色管理`
  - `权限管理`

排序位置：在 `系统设置` 下面。

## 数据库迁移

本次新增 SQL：

- `D:\snow\ustarpay-db\tenant\2026-03-15_tenant_rbac.sql`

新增表：

- `tenant_admin_users`
- `tenant_admin_permission_modules`
- `tenant_admin_permissions`
- `tenant_admin_roles`
- `tenant_admin_role_permissions`
- `tenant_admin_user_roles`

说明：

- 这套表只给租户后台使用。
- 已把现有租户主账号同步为默认超级管理员。
- 默认超级管理员角色会自动拥有全部租户后台权限。

## 登录说明

租户后台现有账号继续可用：

- 账号：`tenant@example.com`
- 密码：`admin123`

登录后权限来源改为：

1. 优先读取 `tenant_admin_users` + `tenant_admin_roles` + `tenant_admin_permissions`
2. 如果没有匹配到租户后台用户，再回退到原 `tenants` 主账号逻辑

## 已打通接口

### RBAC 用户管理

- `GET /tenant-admin/v1/rbac/users`
- `GET /tenant-admin/v1/rbac/users/:id`
- `POST /tenant-admin/v1/rbac/users`
- `PUT /tenant-admin/v1/rbac/users/:id`
- `DELETE /tenant-admin/v1/rbac/users/:id`

### RBAC 角色管理

- `GET /tenant-admin/v1/rbac/roles`
- `GET /tenant-admin/v1/rbac/roles/:id`
- `POST /tenant-admin/v1/rbac/roles`
- `PUT /tenant-admin/v1/rbac/roles/:id`
- `DELETE /tenant-admin/v1/rbac/roles/:id`

### RBAC 权限管理

- `GET /tenant-admin/v1/rbac/modules`
- `POST /tenant-admin/v1/rbac/modules`
- `PUT /tenant-admin/v1/rbac/modules/:id`
- `DELETE /tenant-admin/v1/rbac/modules/:id`
- `GET /tenant-admin/v1/rbac/permissions`
- `GET /tenant-admin/v1/rbac/permissions/:id`
- `POST /tenant-admin/v1/rbac/permissions`
- `PUT /tenant-admin/v1/rbac/permissions/:id`
- `DELETE /tenant-admin/v1/rbac/permissions/:id`

## 本地验证结果

已验证通过：

- 租户账号登录成功
- `RBAC 用户 / 角色 / 权限` 列表接口返回正常
- 可创建测试角色
- 可创建测试租户后台用户
- 可删除测试租户后台用户和角色
- `http://127.0.0.1:3002/rbac/users` 可访问
- `http://127.0.0.1:3002/rbac/roles` 可访问
- `http://127.0.0.1:3002/rbac/permissions` 可访问

## 注意事项

- 当前租户后台里原有的 `用户管理 / KYC管理` 仍然是 App 终端用户维度，不是 RBAC 用户。
- 新增的 `RBAC管理` 是租户后台运营账号维度。
- 两套数据不要混用。

## 2026-03-16 补充更新

1. RBAC管理 已固定为租户后台自己的 RBAC：
   - 菜单：用户管理 / 角色管理 / 权限管理
   - 排序：位于 系统设置 下方
   - 数据源：仅使用租户 RBAC 表，不影响 http://localhost:3000 平台后台 RBAC
2. 权限管理 页面已重构为双栏结构：
   - 左侧：模块卡片列表
   - 右侧：权限列表表格
   - 已预留模块筛选与权限增删改操作
3. 已确认 App 端业务代码中没有额外新增一套 RBAC 逻辑；本次 RBAC 改动只落在租户后台 http://localhost:3002。