# 测试服验收记录

日期：2026-03-18 00:10（Asia/Shanghai）

## 1. 本轮验收目标

围绕明天分享会，重点确认以下链路：

1. 平台后台可登录
2. 租户后台可登录
3. 租户后台用户列表、KYC 列表可打开
4. App 注册、登录可用
5. App 发消息可进入 TGO
6. 客服回消息可回流 App
7. 聊天历史可回查
8. 测试服数据库可通过 SSH 隧道从本地 Navicat 连接

## 2. 验收结果

### 2.1 页面与入口

| 项目 | 地址 | 结果 |
| --- | --- | --- |
| TGO | `http://164.68.124.12/` | 通过 |
| 平台后台 | `http://164.68.124.12/admin/` | 通过 |
| 租户后台 | `http://164.68.124.12/tenant/` | 通过 |
| App Web | `http://164.68.124.12/app/` | 通过 |
| 官网 | `http://164.68.124.12/website/` | 通过 |
| Kong Manager | `http://164.68.124.12:8002/` | 通过 |

### 2.2 登录与业务接口

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 平台后台登录 | 通过 | `admin / admin123` |
| 租户后台登录 | 通过 | `tenant@example.com / admin123` |
| 租户用户列表 | 通过 | `tenant-admin/v1/app-users` 返回 `200` |
| 租户 KYC 列表 | 通过 | `tenant-admin/v1/app-users/kyc` 返回 `200` |
| App 发送验证码 | 通过 | 万能验证码 `888888` |
| App 注册 | 通过 | 公网接口实测 |
| App 登录 | 通过 | 公网接口实测 |
| App 发消息 | 通过 | 返回 `handoff / human` |
| 客服回消息写入历史 | 通过 | 回调成功，历史可查 |

### 2.3 App 聊天验收说明

本轮已完成以下真实链路验收：

1. 新注册 App 用户
2. 新用户登录
3. App 发送消息
4. 回调接口接收客服消息
5. App 聊天历史查询返回客服回复

当前结论：

1. 测试服聊天主链路是通的。
2. 当前模式是人工接待优先，前端会先收到人工接待状态。
3. 客服回复后，消息会回写到 `nh_wallet.app_chat_messages`，并能被 App 历史接口查到。

## 3. Navicat 连接信息

UStarPay 测试服数据库统一改为 SSH 隧道方式访问：

- SSH 主机：`164.68.124.12`
- SSH 端口：`22`
- SSH 用户：`root`
- 数据库主机：`127.0.0.1`
- 数据库端口：`15432`
- 数据库：`nh_wallet`
- 用户名：`postgres`
- 密码：`postgres`
- SSL：关闭

说明：

1. 请不要再使用默认端口 `5432`。
2. 数据库不再建议走公网直连。

## 4. 备注

1. 如果浏览器里残留旧缓存，访问 TGO 前建议先 `Ctrl + F5` 强刷。
2. 如果平台后台偶发跳到 `/admin/403`，优先清浏览器缓存后重新登录。
3. 当前测试服已经达到“可汇报、可演示、可继续联调”的状态。
